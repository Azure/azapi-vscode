{
	"azurerm-restapi_resource": {
		"prefix": "azapires",
		"body": [
			"resource \"azurerm-restapi_resource\" \"${1:test}\" {",
			"  resource_id = \"${2:resource_id}\"",
			"  type        = \"${3:resource_type}\"",
			"  location    = \"${4:west europe}\"",
			"",
			"body = jsonencode({",
			"${5:}",
			"})",
			"}"
		],
		"description": "TODO"
	},
	"azurerm-restapi_patch_resource": {
		"prefix": "azapipatch",
		"body": [
			"for_each = {",
			"\t${1:key} = \"${2:value}\"",
			"}"
		],
		"description": "The for_each meta-argument accepts a map or a set of strings, and creates an instance for each item in that map or set."
	},
	"data.azurerm-restapi_resource": {
		"prefix": "azapidata",
		"body": [
			"for_each = {",
			"\t${1:key} = \"${2:value}\"",
			"}"
		],
		"description": "The for_each meta-argument accepts a map or a set of strings, and creates an instance for each item in that map or set."
	}
}
